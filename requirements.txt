# Patent RAG Engineering Masterclass — pinned dependencies
# Verified installable + executed on Python 3.12 (Windows) and targeting Google Colab.
#
# Policy: the heavy scientific / deep-learning stack that Colab already ships is given
# COMPATIBLE FLOORS (>=) so `pip install -r requirements.txt` never fights Colab's
# preinstalled build; the smaller, more volatile add-ons are PINNED (==) for
# reproducibility. Versions in the trailing comments are what the series was validated on.

# --- core scientific stack (Colab ships these; floors avoid disruptive reinstalls) ---
numpy>=1.26            # validated 2.4.2
pandas>=2.0           # validated 3.0.2
scipy>=1.11           # validated 1.17.1
scikit-learn>=1.3     # validated 1.9.0
matplotlib>=3.7       # validated 3.11.1
pillow>=10.0          # validated 12.3.0
tqdm>=4.65            # validated 4.67.3
regex>=2024.0         # validated 2026.7.19

# --- data modelling ---
pydantic>=2.6          # validated 2.13.3
pydantic-settings>=2.0 # validated 2.14.0

# --- XML / parsing ---
lxml>=5.0              # validated 6.1.1

# --- PDF + OCR (tesseract system binary installed separately; see README) ---
PyMuPDF>=1.24          # validated 1.27.2.3
pdfplumber>=0.11       # validated 0.11.10
pytesseract==0.3.13
reportlab>=4.0         # validated 5.0.1

# --- sparse retrieval ---
rank-bm25==0.2.2

# --- dense retrieval / embeddings / reranking ---
torch>=2.2             # Colab preinstalled; validated on a 2.12 dev build (CPU forced)
transformers>=4.40     # validated 5.16.1
sentence-transformers>=3.0   # validated 6.0.0
faiss-cpu==1.15.0

# --- language identification ---
langdetect==1.0.9

# --- guardrails: PII detection ---
presidio-analyzer>=2.2   # validated 2.2.364
presidio-anonymizer>=2.2 # validated 2.2.364
spacy>=3.7,<4            # validated 3.8.16
# spaCy English model (pinned wheel so `pip install -r` fetches it without a separate step)
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
